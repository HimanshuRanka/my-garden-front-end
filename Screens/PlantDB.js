import React, {useState} from 'react';
import {FlatList, StyleSheet, Text, TouchableOpacity, View} from 'react-native';
import {Button} from "react-native-web";


const PlantDB = ({navigation}) => {
    /* This will be similar to myGarden code. But Can only really implement it once the database is setup */
    /* The logic is also all off atm so im not adding any code here so lets see what happens*/
};

export default PlantDB;